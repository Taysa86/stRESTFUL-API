{{#each posts as |post| }}
<div class="row mb-4 project">
  <div>
    <h2>
      {{post.title}}
    </h2>
    <p>
      {{post.text}}
    </p>
    <p>Created by {{post.user.name}} on {{format_date post.date_created}}</p>
  </div>
</div>
{{/each}}
